const mongoose = require('mongoose');
//mongoose.connect('') //mongodb://localhost/GKUKIB - lehet baj a localhostt-al,
//lehet mást kell megadni, nem tudom, h hogy tesztelem majd, h jó e a connection

module.exports = mongoose;